server:
  port: 9050

spring:
  security:
    oauth2:
      resource-server:
        jwt:
          issuer-uri: http://localhost:8080/realms/${REALM_NAME}

  cloud:
    gateway:
      routes:
        - id: user-tenant-service
          uri: http://localhost:8081
          predicates:
            - Path=/api/v1/users/**, /api/v1/tenants/**, /api/v1/addresses/**

        - id: payment-service
          uri: http://localhost:8082
          predicates:
            - Path=/api/v1/subscriptions/**