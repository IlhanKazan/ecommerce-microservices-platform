server:
  port: 8081

spring:
  datasource:
    url: jdbc:postgresql://postgres:5432/${USER_DB_NAME}
    username: ${USER_DB_USERNAME}
    password: ${USER_DB_PASSWORD}
    hikari:
      minimum-idle: 2
      maximum-pool-size: 10
      connection-timeout: 30000
      idle-timeout: 600000
      max-lifetime: 1800000

  flyway:
    connect-retries: 10
    connect-retries-interval: 3

  kafka:
    producer:
      bootstrap-servers: kafka:9092
      acks: all
      retries: 3
      properties:
        enable.idempotence: true

  data:
    redis:
      host: redis
      port: 6379
      password: ${REDIS_PASSWORD}

  devtools:
    restart:
      enabled: false

security:
  oauth2:
    resourceserver:
      jwt:
        # jwk-set-uri: container DNS'inden JWK çeker, OIDC discovery yapmaz.
        # issuer-uri: OIDC discovery için DEĞİL — sadece iss claim doğrulaması için.
        #   Token'daki iss = http://localhost:8080/... (Keycloak'ın dışa açık URL'i).
        jwk-set-uri: http://keycloak:8080/realms/${REALM_NAME}/protocol/openid-connect/certs
        issuer-uri: http://localhost:8080/realms/${REALM_NAME}

minio:
  url: http://minio:9000
  access-key: ${MINIO_ACCESS_KEY}
  secret-key: ${MINIO_SECRET_KEY}
  bucket-name: ${USER_MINIO_BUCKET}

application:
  config:
    payment-url: http://payment-service:8082
  clients:
    user-tenant:
      url: http://user-tenant-service:8081

management:
  tracing:
    sampling:
      probability: 0.1
  zipkin:
    tracing:
      endpoint: http://zipkin:9411/api/v2/spans

logging:
  level:
    root: WARN
    com.ecommerce.usertenantservice: INFO
